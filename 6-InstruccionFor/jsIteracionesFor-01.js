function mostrar()
{
	var i; 
	/* i=0//inicializacion vx control

	while(i<10)//condicion continuidad
	{
		document.write(i+1 + "<br>");
		i++;// incremento de la variable de control
	} */
	for(i=0 ; i<10 ; i++)// for en incremento.
	{
		document.write( i+1 + "<br>");
	}



}

/*

for(inicializacion vx control; condicion continuidad; incremento de la variable de control)
{
	
}